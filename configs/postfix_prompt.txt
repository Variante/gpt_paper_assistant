Respond with a JSON object containing a single key "papers" whose value is an array of matching papers. Each element must have exactly these keys:
- "ARXIVID": the ArXiv ID string
- "RELEVANCE": integer from 1 to 10 (10 = direct, near-exact match to a specific criterion; 5 = solid but less specific match; omit papers below 5)
- "COMMENT": one sentence naming the matching criterion by number and citing a specific method, benchmark, or result from the abstract

Only include papers that directly and unambiguously satisfy a criterion. Omit all others. If no papers match, return {"papers": []}.
